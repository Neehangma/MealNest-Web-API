{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node scr/server.js",
    "dev": "node -e \"const { spawn } = require('child_process'); const child = spawn(process.execPath, ['dev.js'], { stdio: 'inherit' }); child.on('exit', (code) => process.exit(code)); child.on('error', (err) => { console.error(err); process.exit(1); });\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0"
  }
}
